// eslint-disable-next-line no-unused-vars -- these are used outside
declare namespace NodeJS {
  export interface ProcessEnv {
    FORTY_TWO_CLIENT_ID: string;
    FORTY_TWO_CLIENT_SECRET: string;
  }
}
